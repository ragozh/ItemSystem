
public class Enums{
    public enum AimType 
    {
        FORWARD = 0,
        BACK = 1,
        POINT = 2 // not use
    }

    public enum ItemType
    {
        WEAPON_FORWARD = 0,
        WEAPON_BACK = 1,
        WEAPON_POINT = 2, // not use
        BONUS = 3,
        CONSUMABLE = 4 // not use
    }

    public enum ItemRarity
    {
        COMMON = 0,
        UNCOMMON = 1,
        RARE = 2,
        EPIC = 3,
        LEGENDARY = 4
    }
}